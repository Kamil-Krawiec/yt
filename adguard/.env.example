TZ=Etc/UTC

# Use the server's Tailscale IPv4 here, for example 100.x.y.z.
# The ad-free script creates .env automatically from `tailscale ip -4`.
ADGUARD_BIND_IP=100.x.y.z
ADGUARD_WEB_BIND_IP=100.x.y.z
